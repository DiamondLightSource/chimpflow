// Implement the logic of the chimpflow pages.

class chimpflow__Page extends common__Base {
    constructor(runtime) {
        super(runtime);
    }

    // -------------------------------------------------------------
    // Activate things on the page.

    activate() {

    } // end method
}
